name: Update badges

on:
  schedule:
    # Runs at 2am UTC
    - cron: "20 6 * * *"
jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: SrajanKumarShukla/badge-readme@main
        with:       
          CREDLY_USER: Srajan Shukla
 # optional, but default will use the same from github
